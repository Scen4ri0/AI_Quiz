AI Quiz — интерактивный квиз по искусственному интеллекту с проверкой через GigaChat

Описание
--------
AI Quiz — это веб-приложение для прохождения квиза по теме ИИ и больших языковых моделей.
Ответы пользователя проверяются LLM GigaChat «по смыслу», а все подсказки, объяснения и
итоговый отзыв генерируются моделью в выраженном зумерском стиле
(мемы, сленг, эмодзи — без мата и токсичности).

Проект состоит из backend (FastAPI) и frontend (Vite + vanilla JS).

Основные возможности
--------------------
- 20 вопросов по LLM и ИИ (pre-training, fine-tuning, RLHF, hallucinations и др.)
- Проверка ответов через GigaChat
- Оценка по смыслу, а не по ключевым словам
- Короткие подсказки без спойлеров при ошибках
- Мемный зумерский вайб в объяснениях и итоговом отзыве
- Отслеживание прогресса:
  - сколько вопросов отвечено
  - сколько правильных ответов
  - минимальный порог прохождения (по умолчанию 13 из 20)
- Кнопка «Завершить тест» — получить итоговый отзыв в любой момент
- Сохранение прогресса в localStorage
- Backend не падает, даже если LLM временно недоступен

Структура проекта
-----------------
```
ai_quiz/
├── backend/
│   ├── src/
│   │   ├── main.py            — FastAPI API
│   │   ├── grader.py          — проверка ответов и генерация отзывов через LLM
│   │   ├── gigachat_client.py — клиент GigaChat
│   │   └── questions.json    — вопросы квиза
│   └── .env                  — переменные окружения
│
└── frontend/
    ├── src/
    │   ├── index.html
    │   ├── app.js             — логика UI и состояние квиза
    │   ├── api.js             — HTTP-запросы к backend
    │   └── style.css
    ├── vite.config.js
    └── package.json
```

Требования
----------
Backend:
- Python 3.10+
- Доступ к API GigaChat

Frontend:
- Node.js 18+
- npm (или совместимый пакетный менеджер)

Переменные окружения (backend)
------------------------------
Создай файл backend/.env со следующим содержимым:

```
GIGACHAT_CREDENTIALS=ВАШ_ТОКЕН
GIGACHAT_MODEL=GigaChat-Pro
GIGACHAT_SCOPE=GIGACHAT_API_PERS
GIGACHAT_TEMPERATURE=0.0
GIGACHAT_VERIFY_SSL_CERTS=false

PASS_SCORE=13
DEBUG=false
FRONTEND_ORIGIN=http://localhost:5173
```

Запуск backend
--------------
```
cd ai_quiz/backend
python -m venv venv
source venv/bin/activate

pip install -r requirements.txt
uvicorn src.main:app --reload --port 8000
```

Проверка:
http://localhost:8000/health

Запуск frontend
---------------
```
cd ai_quiz/frontend
npm install
npm run dev
```

Открыть в браузере:
http://localhost:5173

Как работает проверка
---------------------
1. Пользователь вводит короткий ответ (1–2 ключевые идеи).
2. Ответ отправляется в /api/grade.
3. GigaChat возвращает:
   - ok: true / false
   - feedback: текст (подсказка или объяснение).
4. При неверном ответе:
   - нет правильного ответа
   - нет полного объяснения
   - только короткая наводка.
5. При завершении теста вызывается /api/final_feedback:
   - итоговый отзыв полностью генерируется LLM
   - зумерский стиль
   - без технического мусора и метаданных.

Принципы проекта
----------------
- LLM оценивает смысл, а не форму.
- Никакого «слива» правильных ответов.
- Весь пользовательский текст — результат генерации LLM.
- Если LLM недоступен — честное сообщение, без подмены контента.
- Backend всегда возвращает корректный HTTP-ответ.

Идеи для дальнейшего развития
------------------------------
- Экран разбора ошибок после теста
- Повтор только неправильных вопросов
- Таймер на вопросы
- Авторизация пользователей
- Экспорт результата (PDF / ссылка)
- Несколько уровней сложности
- Аналитика и статистика прохождений

Назначение
----------
Проект подходит для:
- обучения основам ИИ и LLM
- демонстрации LLM-оценивания
- внутренних ассессментов
- pet-проектов и демо

Коротко
-------
AI Quiz — это квиз, где тебя оценивает LLM, общается как зумер,
не палит ответы и честно подводит итог 😎

